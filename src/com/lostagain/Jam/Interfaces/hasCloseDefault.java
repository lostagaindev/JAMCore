package com.lostagain.Jam.Interfaces;


/**
 * A widget that implements this interface has its own special close function
 * */

public interface hasCloseDefault {

	 
	 /**
	  * should run CloseDefault(true).
	  */
	 void CloseDefault();
	 
}
