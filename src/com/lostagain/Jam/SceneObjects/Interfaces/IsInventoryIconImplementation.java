package com.lostagain.Jam.SceneObjects.Interfaces;

//temp combo interface. As we separate more into JAM and JAMCore this shouldnt be needed
//(all interfaces should eventually be in JAMCore)
/**
 * 
 * If possible, use IsInventoryIcon, not this. 
 * Once more is moved to the Core, this should only be needed in very few cases outside of the core 
 * @author darkflame
 *
 */
public interface IsInventoryIconImplementation extends IsInventoryItem {


	
	
}
